[pytest]
asyncio_mode = auto
testpaths = tests
# Add the repo root to sys.path so `import secdigest` works regardless of how
# pytest is invoked (`pytest`, `python -m pytest`, CI shell, etc.). Required
# because the project ships without a pyproject.toml / setup.py and so isn't
# pip-installable as a distribution.
pythonpath = .
